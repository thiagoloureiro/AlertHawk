namespace FinOpsToolSample.Models
{
    public static class SubscriptionDefaults
    {
        /// <summary>Default monthly infra support overlay (USD) when not configured per subscription.</summary>
        public const decimal InfraSupportCost = 400m;
    }
}
